const config = {
  plugins: {
    // CRÍTICO: Use o nome 'tailwindcss' como chave, com um objeto vazio ou de opções
    tailwindcss: {},
    // Essencial para compatibilidade com navegadores
    autoprefixer: {},
  },
};

export default config;
